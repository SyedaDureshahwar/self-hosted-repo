# .github/workflows/deploy-self-hosted.yml

name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:

    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: display message
        run: echo "runing on my local machine"